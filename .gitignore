.venv/
gemma_runtime/.venv/
__pycache__/
out/
*.wav
uv.lock
manifest.txt

# CPU-only venv for data generation / evaluation (see scripts/setup_cpu_env.sh)
.venv-cpu/

# Domain term inputs. This repository is public, and the training corpus is a
# list of the exact terms being adapted for, so it must not be committed here.
# Keep it in the private HF dataset repo instead (see CLAUDE.md).
data/generated_sentences.csv
data/generated_sentences_*.csv
data/domain_*
data/*_private.*

# Third-party checkout for the streaming evaluator (cloned by setup, not ours)
vendor/
# Derived from data/test_sentences_100.tsv by build_term_csv/prepare step
data/test_sentences_100.csv
