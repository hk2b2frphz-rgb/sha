# 提案器への申し送り

このファイルの中身は、そのまま Gemma の提案プロンプトに差し込まれる
（`scripts/run_autoresearch.pbs` の `GUIDANCE`、既定でこのファイル）。
探索が自分では気づけない**人間側の事前知識**を書く場所。
karpathy/autoresearch の `program.md` に相当する。

編集して `git push` → サーバーで `git pull` すれば、次のジョブから反映される。
空にしておけば何も渡らない。

## 書き方

- 断定できることだけを書く。推測を書くと提案がそちらに引っ張られる
- 「なぜ」を一緒に書く。理由がないと過度に一般化される
- 効かないと分かったことも書く。同じ設定を何度も試させない

## 現在の申し送り

- 対象は日本語の専門用語を含む読み上げ音声。用語が正しく出るかが最重要で、
  助詞や句読点の誤りは相対的に軽い。
- 学習データは TTS 合成音声、評価データは実録音。話者・収録環境が違うため、
  データ拡張（`augment_*`）はこの差を埋める方向に効く可能性がある。
- 評価データは小さい。スコア差が 0.01 未満の設定を「勝った」と判断しないこと。

<!--
書き足す例:

- lora_r を 128 にすると V100 では OOM することが多い。64 までに留めること。
- beam_size を上げると RTF が悪化する。実運用はストリーミングなので 3 以下が望ましい。
- label_smoothing は 0.05 を超えると用語の再現率が下がった（2026-07 の探索）。
-->
